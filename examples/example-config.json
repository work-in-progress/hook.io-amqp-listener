{
  "queues" : [
    {
      "connection" : "amqp[s]://[user:password@]hostname[:port][/vhost]",
      "queueName" : "my-queue",

      "allowAllByDefault" : true,
      "allow" : ["messageName"],
      "deny" : ["messageName"],
      "nameMapping" : {
        "myAmqpEvent" : "My::Hook::Event"
      }
    }  
  ]
}